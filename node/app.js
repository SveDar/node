const prompt = require('prompt-sync')();

// Ask the user for their name
const name = prompt('What is your name? ');

// Greet the user with a personalized message
console.log(`Hello, ${name}! Welcome to our Node.js application.`);
